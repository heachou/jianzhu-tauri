{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the standard search desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
